<h1>Cast Group</h1>
<% if user_signed_in? %> 
  <%= button_to "Sair", destroy_user_session_path, method: :delete %> 
  <div>Olá, <%= current_user.email %> sem bem vindo a Cast Group.</div> 
<% end %>